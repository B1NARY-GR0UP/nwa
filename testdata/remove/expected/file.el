(defun hello-world ()
  "Print a hello world message."
  (interactive)
  (message "Hello, World!"))

;; Define a variable
(defvar greeting "Hello, Emacs Lisp!"
  "A simple greeting message.")

;; Call the function
(hello-world)